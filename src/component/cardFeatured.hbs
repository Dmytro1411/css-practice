{{#with data}}

<div class="col-4">
	<div class="card">
		<div class="card_image" style="background-image: url({{photo}});"></div>
		<h2 class="title card_title">{{heading}} </h2>
		<p class="text card_text">{{info}}</p>
		<a href="{{href}}">{{linkText}}</a>
	</div>
</div>

{{/with}}